# Catalog 2026-06-02-00 sync

## 배경

issue #703은 `bluetape4k-projects`가 `bluetape4k-dependencies` issue #101에서 자른
source-of-truth catalog를 consume해야 했다.

## 결정

`catalog/2026-06-02-00`을 기본 `bt4k` catalog ref로 사용하고,
`bluetape4k-dependencies`의 shared version alias를 sync한다.

## 결과

Netty, AWS SDK v2, Protobuf, Jackson, Jackson 3, Scrimage의 local catalog drift를
repo-local pin 추가 없이 제거했다.

## 검증

- `scripts/sync-shared-versions.py --workspace /Users/debop/work/bluetape4k --check --summary`
- `scripts/sync-dependabot-ignores.py --workspace /Users/debop/work/bluetape4k --check --summary`

## 향후 지침

shared dependency alert는 먼저 `bluetape4k-dependencies`에서 catalog ref를 자른 뒤,
이 repository의 default catalog ref와 synced alias를 함께 업데이트한다.

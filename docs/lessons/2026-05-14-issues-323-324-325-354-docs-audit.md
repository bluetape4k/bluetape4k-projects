## Issues 323-324-325-354 문서 Audit

배경: Issue #323, #324, #325, #354는 documentation-focused follow-up이었다. 범위는
compressor exception propagation docs, concrete compressor KDoc `@throws`, CHANGELOG
migration note, cache folder reorganization 이후 cache README/KDoc drift였다.

결정: 이전 API와 folder reorganization 변경 뒤의 README/KDoc/CHANGELOG 정확성이라는
같은 user contract surface를 공유하므로 하나의 documentation PR로 묶는다.

결과: `compress()`/`decompress()`의 exception propagation을 보여주도록 `io/io` README
diagram을 갱신하고, `compressOrNull()`/`decompressOrNull()`를 통한 nullable recovery를
문서화했다. `AbstractCompressor`의 넓은 `Exception` KDoc을 교체하고,
algorithm-specific compressor KDoc `@throws`를 추가했으며, cache README pair를
package/import stability note로 확장했다. 오래된 hibernate cache module title과 Gradle
project reference도 수정했다.

검증: #323, #324, #325, #354의 issue body를 확인했다. `git diff --check`, stale
wording search, cache README import symbol audit(44 imports),
`./gradlew projects --no-configuration-cache`, `commons-compress:1.28.0` 대상
dependency insight, `:bluetape4k-io`와 영향받은 cache module 대상 compile을 실행했다.
Claude review는 CHANGELOG wording, Snappy exception KDoc, conditional Apache Zstd
builder risk를 지적했고, 이 항목들은 수정했거나 version/compile evidence로 해소했다.

향후 에이전트: Docs-only cache reorganization follow-up에서는 source folder move와
public package/artifact change를 구분한다. Compressor API에서는 throwing contract와
nullable contract를 나란히 유지해 caller가 명시적인 exception handling 또는 `null`
recovery를 선택할 수 있게 한다.

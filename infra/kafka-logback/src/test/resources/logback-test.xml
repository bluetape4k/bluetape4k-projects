<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <immediateFlush>true</immediateFlush>
        <encoder>
            <!-- @formatter:off -->
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%blue(%.24t)] %yellow(%logger{36}):%line: %msg%n%throwable</pattern>
            <!-- @formatter:on -->
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!--
        KafkaAppender: LogbackIntegrationTest 전용 — topic "logs", bootstrapServers localhost:9093 (KafkaServer 기본 포트)
    -->
    <appender name="Kafka" class="io.bluetape4k.kafka.logback.KafkaAppender">
        <bootstrapServers>localhost:9093</bootstrapServers>
        <topic>logs</topic>
        <keyProvider class="io.bluetape4k.kafka.logback.keyprovider.HostnameKafkaKeyProvider"/>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{ISO8601} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="io.bluetape4k.kafka.logback" level="DEBUG"/>

    <!-- LogbackIntegrationTest 전용: Kafka로만 전송 -->
    <logger name="LogbackIntegrationTest" level="INFO" additivity="false">
        <appender-ref ref="Kafka"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="Console"/>
    </root>

</configuration>

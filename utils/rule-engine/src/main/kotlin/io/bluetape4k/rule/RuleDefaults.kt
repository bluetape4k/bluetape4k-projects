package io.bluetape4k.rule

/**
 * Rule의 기본 이름
 *
 * ```kotlin
 * val name: String = DEFAULT_RULE_NAME // "rule"
 * ```
 */
const val DEFAULT_RULE_NAME = "rule"

/**
 * Rule의 기본 설명
 *
 * ```kotlin
 * val desc: String = DEFAULT_RULE_DESCRIPTION // "rule description"
 * ```
 */
const val DEFAULT_RULE_DESCRIPTION = "rule description"

/**
 * Rule의 기본 우선순위 (priority)
 *
 * ```kotlin
 * val priority: Int = DEFAULT_RULE_PRIORITY // Int.MAX_VALUE - 1
 * ```
 */
const val DEFAULT_RULE_PRIORITY = Int.MAX_VALUE - 1

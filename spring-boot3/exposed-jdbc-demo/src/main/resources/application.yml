spring:
    application:
        name: exposed-spring-data-mvc-demo
    datasource:
        url: jdbc:h2:mem:mvcdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
        driver-class-name: org.h2.Driver
        username: sa
        password:
    exposed:
        generate-ddl: true

logging:
    level:
        io.bluetape4k: DEBUG
        org.jetbrains.exposed: DEBUG

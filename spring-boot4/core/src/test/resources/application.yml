bluetape4k:
    retrofit2:
        services:
            httpbin: ${testcontainers.bluetape-http.httpbinUrl}/
            jsonPlaceHolder: ${testcontainers.bluetape-http.jsonplaceholderUrl}/

management:
    datadog:
        metrics:
            export:
                enabled: false


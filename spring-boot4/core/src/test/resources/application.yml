bluetape4k:
    retrofit2:
        services:
            httpbin: ${testcontainers.httpbin.url}
            jsonPlaceHolder: https://jsonplaceholder.typicode.com

spring:
    cloud:
        compatibility-verifier:
            enabled: false

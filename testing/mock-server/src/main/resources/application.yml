server:
  port: 8888
  http2:
    enabled: true
  tomcat:
    threads:
      max: 16000
    max-connections: 16000
    accept-count: 16000

spring:
  application:
    name: bluetape-mock-server
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mvc:
    async:
      request-timeout: 30000
  threads:
    virtual:
      enabled: true
  cache:
    type: caffeine
    cache-names:
      - html-content
      - fixture-data
      - httpbin-image

logging:
  level:
    io.bluetape4k.mockserver: INFO
